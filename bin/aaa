cd
git clone https://github.com/ytani01/${GIT_NAME}.git
cd ${HOME}/${GIT_NAME}
cp -f *.patch ${ENVBIN}
cp -rf sound ~

if [ ! -d ${HOME}/bin ]; then
    mkdir ${HOME}/bin
fi
cd ${HOME}/${GIT_NAME}/bin
cp -f *.sh *.py ${HOME}/bin
